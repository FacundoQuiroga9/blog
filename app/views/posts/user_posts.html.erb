<div class="container p-0 ">
  <div class="mt-5 py-2 px-5 box-title">
    <h1 class="m-0">Mis publicaciones</h1>
  </div>

  <% @posts.each do |post| %>
    <div class="box-post py-2 px-5">
      <p class="fs-3"><%= post.title %></p>
      <p><%= post.created_at.strftime("%d/%m/%Y") %></p>
    </div>
  <% end %>
</div>
